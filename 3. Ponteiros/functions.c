#include "declarations.h"
#include <stdlib.h>

void createAddress(int *a, int *b, int *c){
    scanf("%d", &a);
    scanf("%d", &b);
    scanf("%d", &c);
}

void getData(int *a, int *b, int *c){

    scanf("%d", &a);
    scanf("%d", &b);
    scanf("%d", &c);
}

void getResult(int *a, int *b, int *c){

}

void showResult(int *a, int *b, int *c){

}